# Partna Backend (FastAPI)
This is a simple backend for the Partna platform using FastAPI.

## Installation
1. Install dependencies:
```
pip install -r requirements.txt
```
2. Run the backend:
```
python main.py
```
3. Access API at `http://127.0.0.1:8000/docs`
